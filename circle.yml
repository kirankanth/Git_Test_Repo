dependencies:
 override:
  - echo "test for dependencies"
  - mvn clean package
test:
 override:
  - echo "Test for circleci"
  - ls
  - cp /home/ubuntu/Git_Test_Repo/target/Test.war $CIRCLE_ARTIFACTS
  - cd $CIRCLE_ARTIFACTS 
  - ls